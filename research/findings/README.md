# Findings — IMMUTABLE

Each file in this folder is a per-source research document produced **verbatim** by an
Opus research sub-agent (see `../subagent-prompt.md`).

**Do not hand-edit these files.** They are the evidence record. If you disagree with a
finding, or want to mark something adopt / adapt / reject, do it in `../thinking/` — not
here.

Naming convention: `<source-slug>.md` (e.g. `alphaevolve.md`, `voyager.md`,
`darwin-godel-machine.md`).
